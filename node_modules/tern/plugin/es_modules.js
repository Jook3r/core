// Stub: es_modules plugin not available in this tern version
(function(mod) {
  if (typeof exports == "object" && typeof module == "object")
    mod(require("../lib/infer"), require("../lib/tern"));
  else if (typeof define == "function" && define.amd)
    define(["../lib/infer", "../lib/tern"], mod);
  else mod(tern, tern);
})(function(infer, tern) {
  "use strict";
  tern.registerPlugin("es_modules", function() {
    return {};
  });
});
